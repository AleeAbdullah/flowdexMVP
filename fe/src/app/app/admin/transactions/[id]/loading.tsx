import { GlassPanel } from '@/components/glass-panel';

export default function AdminTransactionDetailLoading() {
  return (
    <GlassPanel className="p-6 text-sm text-[var(--muted)]">
      Loading admin transaction detail…
    </GlassPanel>
  );
}
